# 8자리 친구 코드 적용
1. Supabase SQL Editor → New query를 여세요.
2. `supabase/migrations/202609120003_friend_codes.sql` 전체를 붙여 넣고 Run을 누르세요.
3. 앱을 새로고침하고 친구 추가 버튼을 누르면 내 8자리 코드와 복사 버튼이 표시됩니다.
4. 상대방에게 코드를 전달하면 친구 추가 검색창에서 검색할 수 있습니다. 영문 대소문자는 구분하지 않습니다.

기존 사용자 UUID, 일정, 친구 관계는 그대로 유지됩니다. 기존 계정과 신규 계정에 코드가 발급되며 SQL을 다시 실행해도 기존 코드는 바뀌지 않습니다. 코드 중복은 UNIQUE 제약과 재시도로 방지합니다. 코드는 로그인 비밀번호가 아니며, 친구 관계는 상대방이 요청을 수락해야 성립합니다.

원격 SQL은 직접 적용하지 않았습니다. 코드 검색 UI의 자동 테스트는 통과했지만 실제 코드 발급과 두 계정 검색은 SQL 적용 후 확인해야 합니다.
