# 친구 시간표 공개 적용
1. 기존 친구 기능 SQL이 적용된 Supabase 프로젝트의 SQL Editor에서 새 쿼리를 만드세요.
2. supabase/migrations/202609120001_friend_timetable.sql 전체를 붙여 넣고 Run을 누르세요.
3. 앱을 새로고침하세요. 각 사용자가 자신의 시간표에서 ‘친구에게 시간표 공개’를 켜면 수락된 친구에게만 보입니다. 기존 시간표는 기본 비공개입니다.
4. 친구 프로필을 열면 전체 화면 캘린더가 나오고, 왼쪽으로 쓸거나 시간표 탭을 누르면 시간표를 봅니다. 오른쪽으로 쓸면 돌아옵니다.

원본 계정 문서의 RLS는 변경하지 않습니다. 전용 읽기 RPC가 친구 관계와 공개 설정을 검사하고 시간표의 표시 필드만 반환합니다. 메모는 공유하지 않습니다. 공개 해제와 친구 삭제는 다음 서버 조회부터 적용됩니다. 열려 있는 친구 화면은 최대 10초마다 다시 조회합니다.

SQL은 파일로 제공했으며 원격 DB에는 직접 적용하지 않았습니다. 실제 두 계정의 시간표 공유/비공개 권한 검증은 SQL 적용 후 필요합니다.
